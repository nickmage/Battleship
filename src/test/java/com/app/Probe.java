package com.app;

import com.app.entities.GameType;
import org.junit.Test;

import java.util.ArrayList;
import org.junit.Assert;

public class Probe {

    @Test
    public void killFromSelected() {


       /* offerForKilling.addPlayer(civilian);
        murder.killFromSelected(offerForKilling);
        List<Player> list = new ArrayList<>(playerList.getPlayerList());
        for (Player p : list) {
            if(!p.isAlive()) {
                playerList.deletePlayer(p);
            }
        }
        Assert.assertEquals(playerList.getPlayerList().size(), 4);*/
    }
}
